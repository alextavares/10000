import 'package:flutter/material.dart';

class OnboardingAiHabitSuggestionsScreen extends StatelessWidget {
  const OnboardingAiHabitSuggestionsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Sugestões de Hábitos'),
      ),
      body: const Center(
        child: Text('Tela de Sugestões de Hábitos com IA (Onboarding 3) - Em construção'),
      ),
    );
  }
}
