import 'package:flutter/material.dart';

class HabitStatisticsTab extends StatelessWidget {
  const HabitStatisticsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}